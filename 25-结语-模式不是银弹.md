# 第 25 章 结语：模式不是银弹

> **学完 24 个模式之后，真正重要的不是记住它们，而是知道什么时候不用它们。**

---

## 1. 我们学了什么

先来一张全家福。全书 24 个模式（23 个 GoF + 简单工厂），按"使用率 ↓、难度 ↑"排成三梯队：

### 高频区：天天用（第 1～13 章）

| 模式 | 一句话 | 记住它 |
|------|--------|--------|
| 单例 Singleton | 全局唯一实例 | 独一份，全局拿 |
| 简单工厂 Simple Factory | 一个函数按参数出对象 | 报名字，出杯子 |
| 策略 Strategy | 算法可插拔 | 换算法不换代码 |
| 迭代器 Iterator | 遍历的标准化 | Python 早就替你实现了 |
| 装饰器 Decorator | 层层包装 | 贴膜加壳不拆机 |
| 外观 Facade | 一个前台搞定一切 | 服务员模式 |
| 工厂方法 Factory Method | 创建交给子类 | 加盟店各做各的招牌 |
| 模板方法 Template Method | 骨架固定，步骤可换 | 做菜流程固定 |
| 观察者 Observer | 有事广播 | 公众号订阅 |
| 适配器 Adapter | 接口转换 | 转换插头 |
| 建造者 Builder | 分步构建复杂对象 | 组装电脑 |
| 代理 Proxy | 经纪人替你办事 | 该拦的拦，该转的转 |
| 责任链 Chain of Responsibility | 层层传递 | 报销审批 |

### 中频区：常用但挑场景（第 14～17 章）

| 模式 | 一句话 | 记住它 |
|------|--------|--------|
| 抽象工厂 Abstract Factory | 一族产品成套生产 | 家具城整套买 |
| 命令 Command | 动作打包可撤销 | 点单小票 |
| 状态 State | 状态决定行为 | 红绿灯 |
| 组合 Composite | 树形结构一视同仁 | 文件夹套文件夹 |

### 低频区：少用但关键时刻很值钱（第 18～24 章）

| 模式 | 一句话 | 记住它 |
|------|--------|--------|
| 原型 Prototype | 克隆对象 | 细胞分裂 |
| 中介者 Mediator | 中间人传话 | 机场塔台 |
| 备忘录 Memento | 状态快照回滚 | 游戏存档 |
| 桥接 Bridge | 双维度各自演化 | 遥控器与电视 |
| 访问者 Visitor | 数据结构不动，操作随便加 | 体检套餐 |
| 享元 Flyweight | 共享细粒度对象 | 图书馆共读 |
| 解释器 Interpreter | 自写迷你语言 | 同声传译 |

---

## 2. 模式不是银弹

1954 年，Fred Brooks 在《人月神话》里提出"没有银弹"——不存在一种技术能让软件开发效率翻十倍。设计模式同样不是银弹。甚至可以说：**模式用错了，比不用更糟。**

### 模式的三种死法

**死法一：为模式而模式（过度设计）**

```python
# 反面教材：两个分支也要上策略模式 + 抽象工厂 + 单例
from abc import ABC

class DiscountStrategy(ABC): ...      # 就两种折扣，硬造一个策略族
class DiscountFactory: ...            # 又造一个工厂
class DiscountRegistry: ...           # 再来个注册表


# 正道：两个分支，一个函数搞定
def discount(price: float, is_vip: bool) -> float:
    return price * 0.8 if is_vip else price * 0.95


print("普通用户：", discount(100, False))
print("VIP 用户：", discount(100, True))
```

运行输出：

```
普通用户： 95.0
VIP 用户： 80.0
```

模式是"问题→方案"的映射，**先有问题，后有模式**。反过来就是"拿着锤子找钉子"。

**死法二：模式套模式，套出意大利面**

一个本该 50 行的功能，硬是凑出 8 个类、5 层抽象。同事接手时内心 OS："这代码是写给我看的，还是写给设计模式书当案例的？"

**死法三：只学形，不学神**

背下了类图，却没理解"为什么"——换了个场景就套错。比如把观察者用在"一次性通知"上（观察者适合"不知道谁在听、可能很多听众"的场景），结果为了注销观察者写了一堆样板代码。

### 什么时候该用模式？

- **痛点真实存在**：你确实遇到了"多处重复的 if-elif 判断类型"（策略/工厂）、"改了 A 必须改 B"（观察者）、"类多到记不住"（外观/桥接）；
- **模式能消除重复或耦合**：模式的价值是"减少变化带来的修改量"，不是"显得专业"；
- **团队能看懂**：模式是沟通词汇，如果团队没人懂，用模式反而增加理解成本。

---

## 3. 如何把模式练成肌肉记忆

看书只能认识模式，**写代码才能会用模式**。三个练习法：

### 方法一：重构练习（推荐）

故意写一段"坏味道"代码（一堆 if-elif、一个 500 行的类、到处 new），然后问自己：

1. 这里的变化点是什么？（加新类型？换算法？通知多方？）
2. 哪个模式对应这个变化点？
3. 重构后，新增需求时还要改多少行？（模式的价值 = 减少的这个行数）

### 方法二：读源码找模式

- 标准库：`logging`（单例注册表 + 桥接 + 责任链）、`asyncio`（工厂 + 状态 + 观察者）、`collections.abc`（协议）、`functools`（装饰器 + 策略）；
- 框架：Django 的中间件（责任链）、CBV 的 dispatch（模板方法）、signals（观察者）、QuerySet（建造者）；Flask 的装饰器路由（装饰器）、请求上下文（单例/代理）。

**带着模式词汇表读源码**，你会发现框架作者们其实都在用这些套路——这正是模式的价值：让你能"读懂"别人的设计意图。

### 方法三：小项目练手

写一个 500 行以内的小项目（CLI 工具、爬虫、记账本），刻意练习 2～3 个模式。写完后问自己：哪些模式是"真需要"，哪些是"炫技"？把炫技的删掉——**学会删模式，才算真会用模式**。

---

## 4. 模式之间的家族关系

### 按目的分：三大家族

```
创建型（怎么造对象）
├── 单例（只造一个）      原型（复制着造）
├── 简单工厂（一个函数造） 工厂方法（子类决定造什么）
├── 抽象工厂（造一整套）   建造者（分步造复杂的）
└── 共同点：把"new"藏起来，让调用方不关心创建细节

结构型（怎么拼对象）
├── 适配器（接口转换）    桥接（双维度解耦）
├── 装饰器（层层包装）    组合（树形结构）
├── 外观（简化门面）      享元（共享细粒度）
└── 代理（控制访问）
   共同点：类与类的"关系"重组，不改行为逻辑

行为型（怎么协作）
├── 策略（换算法）        模板方法（固定流程）
├── 观察者（广播通知）    责任链（链式传递）
├── 命令（动作打包）      状态（状态机）
├── 迭代器（逐个访问）    中介者（居中协调）
├── 备忘录（快照回滚）    访问者（操作外挂）
└── 解释器（迷你语言）
   共同点：对象之间的"消息传递"方式
```

### 常在一起用的"黄金搭档"

- **命令 + 备忘录**：撤销功能双件套（命令记动作、备忘录记状态）；
- **组合 + 迭代器**：遍历树形结构；
- **工厂 + 单例**：全局唯一的工厂（注册表）；
- **观察者 + 中介者**：中介者内部用观察者实现消息分发；
- **模板方法 + 工厂方法**：骨架里调工厂；
- **桥接 + 抽象工厂**：双维度 + 成套创建。

---

## 5. 反模式警告

有正道就有邪道。这几个"反模式"（Anti-pattern）比任何设计错误都常见：

| 反模式 | 症状 | 对应正道 |
|--------|------|----------|
| 上帝对象（God Object） | 一个类什么都管，上千行 | 单一职责 + 外观/中介者合理拆分 |
| 面条代码（Spaghetti） | 逻辑全靠 if 嵌套、goto 式跳转 | 策略 + 状态 + 责任链梳理流程 |
| 复制粘贴编程（Copy-Paste） | 同一段逻辑出现 N 次 | 模板方法 + 策略提取公共部分 |
| 万能胶水类（Blob） | 工具方法全堆一个类 | 外观（对外） + 模块化（对内） |
| 过早优化 | 数据量 100 条就上享元/缓存 | YAGNI：先跑起来，瓶颈出现再优化 |
| 模式滥用（Pattern Abuse） | 注释里全是"这里用了 X 模式" | 回到第 2 节：先有问题，后有模式 |

---

## 6. 继续学习的地图

如果这本书让你对设计模式产生了兴趣（或至少不害怕了），下面是继续深入的路标：

- **本书附录 A：24 模式速查表**：学完 24 章后，用它做"场景→模式"反向检索和面试前速览；
- **《设计模式：可复用面向对象软件的基础》**（GoF 原书）：模式圣经，C++/Smalltalk 示例，Python 读者建议对照本书理解；
- **《Head First 设计模式》**：轻松幽默的入门经典，Java 示例；
- **《重构：改善既有代码的设计》**（Martin Fowler）：模式和重构是双胞胎——重构是"怎么改"，模式是"改成什么样"；
- **Refactoring Guru 网站**（refactoring.guru）：图解模式 + 各种语言代码示例，含 Python；
- **Python 官方文档**：`abc`、`collections.abc`、`functools`、`itertools` 模块文档——读标准库源码是最便宜的"大师课"；
- **Django / Flask 源码**：真实世界的模式集大成者；
- **《Python 源码剖析》/ CPython 源码**：想挑战的话，看看解释器自己怎么用模式（迭代器、单例、享元、访问者……）。

---

## 7. 最后的话

还记得导读里那句话吗？

> 设计模式是"思想套路"的词汇表，Python 让它变得更简单，但"识别问题、选对套路"的能力，任何语言都替不了你。

现在你手里有 24 个套路了。但请记住三件事：

1. **模式是词汇，不是处方**：它们的价值在于沟通和启发，不在于"必须套用"；
2. **Python 让模式更轻**：函数、协议、生成器、装饰器——很多 GoF 模式在 Python 里退化成几行代码，这是好事，不是偷工减料；
3. **最好的模式是没有模式**：当你写出清晰、内聚、低耦合的代码，别人看不出任何模式——那才是模式的最高境界：**化套路于无形**。

> **全书写完了，但你的代码之旅才刚刚开始。去写点烂代码，再把它改好——那是学习模式唯一的路。**

---

*《Python 设计模式修炼手册》完。感谢阅读。*

*后记：本书所有代码示例均通过 `scripts/verify_code.py` 实际运行验证（Python 3.12），"运行输出"全部与真实输出一致。你可以放心复制每一段代码。*
