window.BOOK_DATA = window.BOOK_DATA || {};
window.BOOK_DATA['00-导读-设计模式入门'] = '# 第 0 章 导读：设计模式是什么，以及为什么在 Python 里它们长得不一样\n\n> **本章是整本书的地基。** 花 30 分钟读完它，后面的 24 个模式你会学得又快又稳。\n> 如果你已经熟悉面向对象和 SOLID 原则，可以直接跳到 0.5 节——那节讲的是"Python 为什么让设计模式变了样"，是本书与市面上其他设计模式书最大的不同。\n\n---\n\n## 0.1 设计模式：建筑师的图纸，程序员的套路\n\n1977 年，建筑学家克里斯托弗·亚历山大写了一本书叫《建筑模式语言》，里面收集了 253 种建筑设计的"套路"：什么样的门厅让人舒服、什么样的窗户采光好。建筑师们发现：**好的设计是有规律可循的**，而且这些规律可以命名、可以传授。\n\n1994 年，四个程序员（Erich Gamma、Richard Helm、Ralph Johnson、John Vlissides，江湖人称"四人组"GoF）把这套思路搬到了软件工程里，出版了《设计模式：可复用面向对象软件的基础》。书里总结了 **23 个经典设计模式**——也就是"面向对象设计中反复出现的问题的成熟解决方案"。这本书的地位有多高？业界直接管它叫"**GoF 圣经**"。你面试时听到的"单例""观察者""工厂"这些黑话，全出自这里。\n\n但请注意一个容易被忽略的事实：**设计模式不是发明出来的，是总结出来的**。GoF 没有凭空造出这些模式，他们只是把当时优秀代码里反复出现的结构提炼成了词汇。换句话说：\n\n> 设计模式 = 前人踩过无数坑之后，沉淀下来的"套路词汇表"。\n\n它让你能对同事说"这里用观察者"，而不是解释十分钟；让你面对一个陌生问题时，脑子里能自动浮现出"这个问题我见过，属于某某套路"。\n\n当然，模式也不是万能钥匙。它不是"复制粘贴的代码模板"，而是"解决某类问题的思想框架"。**先有问题，后有模式**，顺序千万别反——反了就成了"拿着锤子找钉子"。\n\n---\n\n## 0.2 一个模式有哪四要素\n\nGoF 给每个模式规定了四个组成部分，本书每一章都会按这个骨架来讲：\n\n| 要素 | 作用 | 打个比方 |\n|------|------|----------|\n| **模式名** | 一个词汇，方便沟通 | 说"单例"，大家就知道是"全局唯一实例" |\n| **问题** | 这个模式解决什么场景下的什么问题 | "多个对象各持一份配置，改起来互相打架" |\n| **解决方案** | 抽象层面上的设计：类怎么分、对象怎么协作 | "让类只产生一个实例，并提供全局访问点" |\n| **后果** | 用了它得到什么好处、付出什么代价 | "省内存、访问统一，但引入全局状态" |\n\n记住这四个要素，你就能看穿任何"包装成新名词"的伪模式：**讲不清"问题"和"后果"的，都不是模式。**\n\n---\n\n## 0.3 面向对象十分钟速览\n\n如果你已经会写类，这一节可以跳读；如果你有点生疏，这一节就是你的救命稻草。\n\n### 类、对象、继承、多态\n\n面向对象编程（OOP）的核心思想：把数据和行为打包在一起，成为"对象"；通过"类"来批量生产结构相同的对象；通过"继承"复用代码；通过"多态"让不同对象对同一句话做出不同回应。\n\n```python\nclass Animal:\n    """所有动物的基类"""\n\n    def __init__(self, name: str):\n        self.name = name\n\n    def speak(self) -> str:\n        return "..."\n\n\nclass Dog(Animal):\n    def speak(self) -> str:\n        return "汪汪！"\n\n\nclass Cat(Animal):\n    def speak(self) -> str:\n        return "喵～"\n\n\ndef let_it_talk(animal: Animal) -> None:\n    """多态：不管传入什么动物，都能让它开口"""\n    print(f"{animal.name}说：{animal.speak()}")\n\n\nlet_it_talk(Dog("旺财"))\nlet_it_talk(Cat("咪咪"))\nlet_it_talk(Animal("无名氏"))\n```\n\n运行输出：\n\n```\n旺财说：汪汪！\n咪咪说：喵～\n无名氏说：...\n```\n\n看到了吗？`let_it_talk` 根本不在乎传进来的是狗还是猫，只要它是 `Animal` 就行——这就是**多态**。设计模式里 80% 的套路，本质上都是在玩"多态"这个把戏。\n\n### 鸭子类型：Python 的多态更狂野\n\n传统 OOP（Java、C++）要求"你必须是 Animal 的子类，我才能让你开口"。Python 不一样，它信奉**鸭子类型**：\n\n> 如果它走起来像鸭子、叫起来像鸭子，那它就是鸭子。\n\n```python\ndef let_it_talk(animal) -> None:\n    """只要对象有 speak 方法，就能让它开口（鸭子类型）"""\n    print(f"{animal.name}说：{animal.speak()}")\n\n\nclass Robot:\n    """机器人不是 Animal 的子类，但它有 speak 方法"""\n\n    def __init__(self, name: str):\n        self.name = name\n\n    def speak(self) -> str:\n        return "哔哔——电池电量 99%"\n\n\nlet_it_talk(Robot("R2-D2"))  # 照样能说话！\n```\n\n运行输出：\n\n```\nR2-D2说：哔哔——电池电量 99%\n```\n\n`Robot` 和 `Animal` 毫无血缘关系，但 Python 根本不管。**只要对象有你要的方法，它就是"那种对象"**。这就是为什么 Python 里很多模式可以省略"抽象基类"这一步——后面你会反复看到这个现象。\n\n### 协议：Python 版的"接口"\n\nPython 用"协议"（Protocol）来描述"拥有哪些方法"的约定。比如"可迭代协议"就是"有 `__iter__` 方法"，"序列协议"就是"有 `__len__` 和 `__getitem__`"。\n\n```python\nclass Playlist:\n    """一个能像列表一样被遍历的歌单"""\n\n    def __init__(self, songs: list[str]):\n        self._songs = songs\n\n    def __len__(self) -> int:\n        return len(self._songs)\n\n    def __getitem__(self, index: int) -> str:\n        return self._songs[index]\n\n\npl = Playlist(["晴天", "七里香", "稻香"])\nfor song in pl:          # 没有 __iter__，但 Python 会退而求其次用 __getitem__\n    print(f"播放：{song}")\nprint(f"共 {len(pl)} 首")\n```\n\n运行输出：\n\n```\n播放：晴天\n播放：七里香\n播放：稻香\n共 3 首\n```\n\n**"实现协议"比"继承类"更轻、更灵活**——这是理解 Python 版设计模式的关键钥匙。\n\n---\n\n## 0.4 六大设计原则：模式的"底层操作系统"\n\n如果说 23 个模式是招式，那设计原则就是内功。招式可以忘，内功不能丢。这一节介绍六个最重要的原则，后面的每一章都会反复提到它们。\n\n### ① 单一职责原则（SRP）\n\n> **一个类应该只有一个改变的理由。**\n\n翻译成大白话：**一个类只干一件事。** 如果"用户"类既要管用户数据，又要管发邮件，又要管生成报表——改邮件模板会动到用户类，改报表格式也会动到用户类，这个类就成了"改谁都疼"的万能膏药。\n\n```python\n# 反面教材：一个类包揽所有事\nclass User:\n    def __init__(self, name: str, email: str):\n        self.name = name\n        self.email = email\n\n    def save_to_db(self) -> None:          # 职责 1：存储\n        print(f"把 {self.name} 存入数据库")\n\n    def send_welcome_email(self) -> None:  # 职责 2：邮件\n        print(f"给 {self.email} 发欢迎邮件")\n\n    def export_report(self) -> None:       # 职责 3：报表\n        print(f"导出 {self.name} 的报表")\n\n\n# 正确姿势：拆成三个各司其职的类\nclass User:\n    def __init__(self, name: str, email: str):\n        self.name = name\n        self.email = email\n\n\nclass UserRepository:\n    def save(self, user: User) -> None:\n        print(f"把 {user.name} 存入数据库")\n\n\nclass EmailService:\n    def send_welcome(self, user: User) -> None:\n        print(f"给 {user.email} 发欢迎邮件")\n\n\nuser = User("小明", "xiaoming@example.com")\nUserRepository().save(user)\nEmailService().send_welcome(user)\n```\n\n运行输出：\n\n```\n把 小明 存入数据库\n给 xiaoming@example.com 发欢迎邮件\n```\n\n### ② 开闭原则（OCP）\n\n> **对扩展开放，对修改关闭。**\n\n意思是：加新功能时，**尽量不改老代码，只加新代码**。老代码一旦改出 bug，受累的是全体用户；新代码出了问题，最多是新增功能不可用。\n\n```python\n# 反面教材：每加一种形状，就要改一次 draw_all\ndef draw_all_v1(shapes: list) -> None:\n    for s in shapes:\n        if s["type"] == "circle":\n            print("画圆")\n        elif s["type"] == "square":\n            print("画方")\n        # 加三角形？改这里！\n\n\n# 正确姿势：让每种形状自己会画，新增形状不用动旧代码\nclass Shape:\n    def draw(self) -> None: ...\n\n\nclass Circle(Shape):\n    def draw(self) -> None:\n        print("画一个圆 ⭕")\n\n\nclass Square(Shape):\n    def draw(self) -> None:\n        print("画一个方 ⬜")\n\n\nclass Triangle(Shape):  # 新形状 = 新类，旧代码一行不用改\n    def draw(self) -> None:\n        print("画一个三角 🔺")\n\n\ndef draw_all(shapes: list[Shape]) -> None:\n    for s in shapes:\n        s.draw()\n\n\ndraw_all([Circle(), Square(), Triangle()])\n```\n\n运行输出：\n\n```\n画一个圆 ⭕\n画一个方 ⬜\n画一个三角 🔺\n```\n\n> 设计模式里那些"抽象基类 + 子类实现"的结构，十有八九就是为了开闭原则服务的。\n\n### ③ 里氏替换原则（LSP）\n\n> **子类必须能替换父类，而且程序行为不被打乱。**\n\n翻译：**"是"字别乱用。** 如果"企鹅"不是"会飞的鸟"，那"企鹅"就不该继承"会飞的鸟"。\n\n```python\nclass Bird:\n    def fly(self) -> str:\n        return "飞走了"\n\n\nclass Sparrow(Bird):  # 麻雀：会飞，没问题\n    pass\n\n\n# 反面教材：企鹅不会飞，硬继承 Bird 就得改写 fly\nclass Penguin(Bird):\n    def fly(self) -> str:\n        raise NotImplementedError("企鹅不会飞！")\n\n\n# 正确姿势：把"会飞"拆成独立的抽象\nclass Bird:\n    pass\n\n\nclass FlyingBird(Bird):\n    def fly(self) -> str:\n        return "飞走了"\n\n\nclass Penguin(Bird):\n    def swim(self) -> str:\n        return "游泳冠军 🐧"\n\n\ndef make_it_fly(bird: FlyingBird) -> None:\n    print(bird.fly())\n\n\nmake_it_fly(Sparrow())\n```\n\n运行输出：\n\n```\n飞走了\n```\n\n> 现实教训：很多 bug 都源于"继承关系想当然"。判断标准很简单——**子类替换父类后，调用方完全无感知，才算合格。**\n\n### ④ 接口隔离原则（ISP）\n\n> **客户端不应该被迫依赖它用不到的接口。**\n\n翻译：**接口要小而专，别搞大而全。** Python 里没有 Java 那种强制"接口"，但协议（Protocol）同样遵循这个原则。\n\n```python\n# 反面教材：一个"全能打印机"接口，普通打印机被迫实现传真\nclass MultiFunctionPrinter:\n    def print(self) -> None: ...\n    def fax(self) -> None: ...\n    def scan(self) -> None: ...\n\n\nclass BasicPrinter(MultiFunctionPrinter):\n    def print(self) -> None:\n        print("打印中...")\n\n    def fax(self) -> None:\n        raise NotImplementedError("我没有传真功能！")\n\n    def scan(self) -> None:\n        raise NotImplementedError("我没有扫描功能！")\n\n\n# 正确姿势：接口拆开\nclass Printer:\n    def print(self) -> None: ...\n\n\nclass Scanner:\n    def scan(self) -> None: ...\n\n\nclass BasicPrinter(Printer):\n    def print(self) -> None:\n        print("打印中...")\n\n\nbasic = BasicPrinter()\nbasic.print()\n```\n\n运行输出：\n\n```\n打印中...\n```\n\n### ⑤ 依赖倒置原则（DIP）\n\n> **高层模块不应该依赖低层模块，两者都应该依赖抽象。**\n\n翻译：**别让"业务逻辑"直接耦合"具体工具"**，中间垫一层抽象，方便替换。这就是"依赖注入"的思想雏形，第 7 章工厂方法、第 14 章抽象工厂都会用到它。\n\n```python\n# 反面教材：业务代码直接 new 一个 MySQL 连接\nclass ReportGeneratorV1:\n    def generate(self) -> None:\n        db = MySQLDatabase()          # 想换成 PostgreSQL？改业务代码！\n        data = db.query("SELECT * FROM sales")\n        print(f"生成报表：{data}")\n\n\nclass MySQLDatabase:\n    def query(self, sql: str) -> str:\n        return f"MySQL 返回了 {sql} 的结果"\n\n\n# 正确姿势：业务代码只认抽象接口\nclass Database:\n    def query(self, sql: str) -> str: ...\n\n\nclass MySQLDatabase(Database):\n    def query(self, sql: str) -> str:\n        return f"MySQL 返回了 {sql} 的结果"\n\n\nclass PostgreSQLDatabase(Database):\n    def query(self, sql: str) -> str:\n        return f"PostgreSQL 返回了 {sql} 的结果"\n\n\nclass ReportGenerator:\n    def __init__(self, db: Database):   # 依赖注入：数据库从外面给\n        self._db = db\n\n    def generate(self) -> None:\n        data = self._db.query("SELECT * FROM sales")\n        print(f"生成报表：{data}")\n\n\nReportGenerator(MySQLDatabase()).generate()\nReportGenerator(PostgreSQLDatabase()).generate()\n```\n\n运行输出：\n\n```\n生成报表：MySQL 返回了 SELECT * FROM sales 的结果\n生成报表：PostgreSQL 返回了 SELECT * FROM sales 的结果\n```\n\n### ⑥ 组合优于继承\n\n> **能用"有一个"解决，就别用"是一个"。**\n\n继承是"是"的关系（狗是动物），组合是"有"的关系（车有引擎）。继承太深会导致"菱形继承地狱"，而组合灵活得多。\n\n```python\nclass Engine:\n    def start(self) -> str:\n        return "引擎轰鸣 🚗"\n\n\nclass Wheels:\n    def roll(self) -> str:\n        return "车轮滚动"\n\n\n# 反面教材：继承狂魔\nclass Vehicle:\n    pass\n\n\nclass Car(Vehicle):\n    pass\n\n\nclass GasCar(Car):\n    pass\n\n\nclass GasCarWithNitro(GasCar):   # 这种类爆炸你怕不怕\n    pass\n\n\n# 正确姿势：组合\nclass Car:\n    def __init__(self):\n        self.engine = Engine()    # 车"有一个"引擎\n        self.wheels = Wheels()    # 车"有一个"轮子\n\n    def drive(self) -> str:\n        return f"{self.engine.start()}，{self.wheels.roll()}"\n\n\nprint(Car().drive())\n```\n\n运行输出：\n\n```\n引擎轰鸣 🚗，车轮滚动\n```\n\n> **六个原则背不全没关系，记住这句话就够了：** 高内聚、低耦合——每个类专心干自己的事，类之间少说废话，靠"抽象"传递消息。\n\n---\n\n## 0.5 Python 让设计模式"变形"了\n\n这是本书最核心的观点。GoF 写书时的主流语言是 C++ 和 Smalltalk，后来 Java 把它发扬光大。但**Python 是一门"动态的、函数式的、鸭子类型的"语言**，同样的思想，在 Python 里往往可以写得更简单——甚至有些模式在 Python 里"消失"了。\n\n### 现象一：函数是一等公民，很多"类模式"退化成"函数模式"\n\nGoF 时代，想实现"策略"必须定义接口、写一堆类。在 Python 里，一个函数就够了。\n\n```python\n# Java 风格：策略需要类和接口；Python 风格：直接传函数\ndef discount_vip(price: float) -> float:\n    return price * 0.8\n\n\ndef discount_normal(price: float) -> float:\n    return price * 0.95\n\n\ndef checkout(price: float, discount) -> float:\n    return discount(price)\n\n\nprint(checkout(100, discount_vip))      # 把函数当参数传\nprint(checkout(100, discount_normal))\n```\n\n运行输出：\n\n```\n80.0\n95.0\n```\n\n第 3 章策略模式里你会看到，Python 里"策略"常常就是一个函数或一个 `lambda`。\n\n### 现象二：鸭子类型让"接口"变得可有可无\n\nJava 里 Adapter、Proxy 等模式需要显式定义接口；Python 里只要"长得像"就行。你甚至不需要让被适配的类继承任何东西。\n\n### 现象三：语法糖直接内建了某些模式\n\n- **迭代器模式**：`for x in ...` 本身就是迭代器，你天天在用（第 4 章）。\n- **装饰器模式**：`@staticmethod`、`@property`、`@functools.lru_cache`——Python 连语法都给你了（第 5 章）。\n- **单例模式**：模块天然只加载一次，`import` 本身就是单例（第 1 章）。\n- **外观模式**：一个组织良好的模块文件，本身就是外观（第 6 章）。\n\n```python\nimport functools\n\n@functools.lru_cache(maxsize=None)\ndef fib(n: int) -> int:\n    """带缓存的斐波那契——装饰器模式 + 备忘录思想，一行搞定"""\n    return n if n < 2 else fib(n - 1) + fib(n - 2)\n\n\nprint(fib(50))\nprint(f"缓存命中信息：{fib.cache_info()}")\n```\n\n运行输出：\n\n```\n12586269025\n缓存命中信息：CacheInfo(hits=48, misses=51, maxsize=None, currsize=51)\n```\n\n### 现象四：Python 的"动态"让某些模式更简单，也让某些模式更危险\n\n动态语言可以随时给对象加属性、改方法，这让"代理""装饰"实现起来异常轻松（`__getattr__` 一个钩子就搞定）；但代价是**没有编译器帮你检查错误**，模式用错了也没有编译期报错。所以本书会特别强调每个模式的"常见误区"。\n\n---\n\n## 0.6 本书怎么读\n\n### 排序逻辑\n\n全书 24 个模式按**"实际开发使用率为主、难易程度为辅"**排序：\n\n1. **高频区（第 1～13 章）**：日常开发几乎天天遇到，先学先用；\n2. **中频区（第 14～17 章）**：常见于中型项目与框架源码；\n3. **低频区（第 18～24 章）**：用得少，但在特定场景、面试和源码阅读中价值极高。\n\n每章开头都有"难度"和"使用率"两个星级，一目了然。\n\n### 每章结构\n\n每章固定 10 个小节：故事引子 → 模式定义 → Python 实现 → Python 特有玩法 → 真实世界应用 → 优缺点 → 模式关系 → 常见误区 → 练习题 → 口诀小结。\n\n全书末尾还有**附录 A：24 模式速查表**（场景→模式对照、选择决策树、总表、面试题、口诀汇总），适合学完后复习和面试前速览。\n\n### 代码约定（重要！）\n\n- 书中所有 `python` 代码块**完整、自包含、可直接运行**，且只用**标准库**；\n- 代码块运行后会在终端打印关键输出，展示效果；\n- 全书代码已经通过 `scripts/verify_code.py` 脚本**逐块实际运行验证**；\n- 想自己跑某段代码？直接复制到任意 `.py` 文件或 REPL 里执行即可。\n\n### 学习建议\n\n1. **先跑代码，再看解释**：动手跑一遍比读十遍记得牢。\n2. **每章做完练习题**：答案给了代码，但先别急着看。\n3. **带着问题读"真实世界"小节**：那部分是你面试时"我实际用过"的谈资来源。\n4. **别贪多**：一天 2～3 个模式正好，第 1～13 章值得精读两遍。\n\n准备好了吗？让我们从全宇宙使用率最高的模式——**单例**——开始。\n\n---\n\n*本章金句：设计模式是"思想套路"的词汇表，Python 让它变得更简单，但"识别问题、选对套路"的能力，任何语言都替不了你。*';
